# Offline Mode - Full Mock API
# Copy this to .env.local to run entirely offline without backend

# Leave VITE_API_URL empty to use mock data only
VITE_API_URL=

# Optional: Explicitly enable mock mode (not needed if VITE_API_URL is empty)
# VITE_USE_MOCK_API=true

# Optional: Gemini API key for AI search (can work offline without it)
# VITE_GEMINI_API_KEY=your_key_here
